apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: goormthon-5
  labels:
    app: backend
type: Opaque
stringData:
  # Database password (base64 인코딩 없이 직접 입력)
  DB_PASSWORD: "postgres123"

  # 필요한 경우 추가 시크릿
  # JWT_SECRET: "your-jwt-secret-here"
  # API_KEY: "your-api-key-here"
